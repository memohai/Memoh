#!/bin/sh
TOOLKIT=/opt/memoh/toolkit
if [ -f /lib/ld-musl-*.so.1 ] 2>/dev/null; then
  NODEDIR="$TOOLKIT/node-musl"
else
  NODEDIR="$TOOLKIT/node-glibc"
fi
export PATH="$NODEDIR/bin:$PATH"
exec "$NODEDIR/bin/npm" "$@"
