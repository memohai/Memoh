# 记忆

**记忆（Memory）** 是 Memoh 为每个 Bot 提供的长期知识系统。

## 工作原理

- 从对话中提取重要事实
- 记忆被存储和索引
- 需要时通过语义相似度检索相关记忆

Memoh 结合向量检索和关键词检索，以获得更好的召回率和精确度。

## 为什么重要

记忆实现了长时间跨度的连续性，使 Bot 能够在短提示词窗口之外维持上下文。

## Web UI 路径

- `Bots > 选择一个 Bot > Memory`
