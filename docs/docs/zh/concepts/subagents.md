# 子代理

**子代理（Subagents）** 是 Bot 可以委派任务的专门辅助代理。

## 核心思想

- 主 Bot 充当协调者
- 子代理专注于更具体的任务
- 结果被合并回主工作流

这支持将复杂请求分解为更小、更可靠的执行单元。

## 为什么重要

子代理架构提升了多步骤任务的可扩展性，并帮助按角色分离职责。

## Web UI 路径

- `Bots > 选择一个 Bot > Subagents`
