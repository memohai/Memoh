# MCP

**MCP（Model Context Protocol）** 将 Memoh Bot 连接到外部工具服务。

## MCP 带来什么

- 标准化的工具发现和调用
- 无需修改核心 Bot 代码即可扩展远程能力
- 每个 Bot 灵活的集成配置

Memoh 支持 MCP 连接，使 Bot 可以通过统一协议访问外部系统。

## 为什么重要

MCP 使能力扩展模块化。你可以在最小耦合的情况下添加或替换工具后端。

## Web UI 路径

- `Bots > 选择一个 Bot > MCP`
