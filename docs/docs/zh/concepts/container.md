# 容器

每个 Bot 运行在自己的**隔离容器**中。

## 隔离带来什么

- 每个 Bot 独立的文件系统
- 独立的运行时进程空间
- 受控的生命周期（创建/启动/停止/删除）

这防止了一个 Bot 干扰另一个 Bot 的执行环境。

## 为什么重要

容器隔离是允许 Bot 安全地并行运行工具、命令和文件操作的基础。

## Web UI 路径

- `Bots > 选择一个 Bot > Container`
