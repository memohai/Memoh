// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: subagents.sql

package sqlc

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createSubagentConfig = `-- name: CreateSubagentConfig :one
INSERT INTO subagent_configs (
  session_id,
  model_uuid,
  model_id,
  provider_name,
  forked,
  parent_messages
) VALUES (
  $1, $2, $3, $4, $5, $6
)
RETURNING team_id, session_id, model_uuid, model_id, provider_name, forked, parent_messages, created_at, updated_at
`

type CreateSubagentConfigParams struct {
	SessionID      pgtype.UUID `json:"session_id"`
	ModelUuid      pgtype.UUID `json:"model_uuid"`
	ModelID        string      `json:"model_id"`
	ProviderName   string      `json:"provider_name"`
	Forked         bool        `json:"forked"`
	ParentMessages []byte      `json:"parent_messages"`
}

func (q *Queries) CreateSubagentConfig(ctx context.Context, arg CreateSubagentConfigParams) (SubagentConfig, error) {
	row := q.db.QueryRow(ctx, createSubagentConfig,
		arg.SessionID,
		arg.ModelUuid,
		arg.ModelID,
		arg.ProviderName,
		arg.Forked,
		arg.ParentMessages,
	)
	var i SubagentConfig
	err := row.Scan(
		&i.TeamID,
		&i.SessionID,
		&i.ModelUuid,
		&i.ModelID,
		&i.ProviderName,
		&i.Forked,
		&i.ParentMessages,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getSubagentConfig = `-- name: GetSubagentConfig :one
SELECT team_id, session_id, model_uuid, model_id, provider_name, forked, parent_messages, created_at, updated_at
FROM subagent_configs
WHERE session_id = $1
`

func (q *Queries) GetSubagentConfig(ctx context.Context, sessionID pgtype.UUID) (SubagentConfig, error) {
	row := q.db.QueryRow(ctx, getSubagentConfig, sessionID)
	var i SubagentConfig
	err := row.Scan(
		&i.TeamID,
		&i.SessionID,
		&i.ModelUuid,
		&i.ModelID,
		&i.ProviderName,
		&i.Forked,
		&i.ParentMessages,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const upsertSubagentConfig = `-- name: UpsertSubagentConfig :one
INSERT INTO subagent_configs (
  session_id,
  model_uuid,
  model_id,
  provider_name,
  forked,
  parent_messages
) VALUES (
  $1, $2, $3, $4, $5, $6
)
ON CONFLICT (session_id) DO UPDATE SET
  model_uuid = EXCLUDED.model_uuid,
  model_id = EXCLUDED.model_id,
  provider_name = EXCLUDED.provider_name,
  forked = EXCLUDED.forked,
  parent_messages = EXCLUDED.parent_messages,
  updated_at = now()
RETURNING team_id, session_id, model_uuid, model_id, provider_name, forked, parent_messages, created_at, updated_at
`

type UpsertSubagentConfigParams struct {
	SessionID      pgtype.UUID `json:"session_id"`
	ModelUuid      pgtype.UUID `json:"model_uuid"`
	ModelID        string      `json:"model_id"`
	ProviderName   string      `json:"provider_name"`
	Forked         bool        `json:"forked"`
	ParentMessages []byte      `json:"parent_messages"`
}

func (q *Queries) UpsertSubagentConfig(ctx context.Context, arg UpsertSubagentConfigParams) (SubagentConfig, error) {
	row := q.db.QueryRow(ctx, upsertSubagentConfig,
		arg.SessionID,
		arg.ModelUuid,
		arg.ModelID,
		arg.ProviderName,
		arg.Forked,
		arg.ParentMessages,
	)
	var i SubagentConfig
	err := row.Scan(
		&i.TeamID,
		&i.SessionID,
		&i.ModelUuid,
		&i.ModelID,
		&i.ProviderName,
		&i.Forked,
		&i.ParentMessages,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
